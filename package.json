{
  "name": "node_starter",
  "version": "1.0.0",
  "description": "A node starter project",
  "main": "dist/index.js",
  "scripts": {
    "sass": "sass src/index.scss dist/index.css",
    "terser": "terser dist/index.js -o dist/index.min.js --config-file minify.json",
    "build": "tsc && npm run sass && npm run terser",
    "start": "",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.34.1",
    "terser": "^5.7.0",
    "tslint": "^6.1.3",
    "typescript": "^4.3.2",
    "uglify-js": "^3.13.9"
  }
}
